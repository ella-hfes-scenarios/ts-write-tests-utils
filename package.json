{
  "name": "ts-write-tests-utils",
  "version": "1.0.0",
  "description": "HFES Scenario: Write tests for a utility library",
  "scripts": {
    "build": "tsc",
    "test": "jest --config jest.config.js",
    "test:visible": "jest --config jest.config.js --testPathPattern=tests/visible"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.2"
  }
}
